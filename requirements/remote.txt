-r base.txt

# Retrieval & Reranking
faiss-cpu>=1.8.0
pylate>=0.2.0
bm25s>=0.2.0
pyspellchecker>=0.8.0

# Inference Engine
vllm>=0.6.0
torch-tensorrt  # TensorRT backend for dense teacher encoding (phase2)

# Utilities
aiohttp>=3.10.0
packaging>=24.2
pytest>=8.0.0
# flash-attn optional: speeds up vLLM on Ampere+ GPUs
# pip install flash-attn --no-build-isolation
